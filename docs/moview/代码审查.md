## 2026-07-10 10:35

## 代码审查报告

### 审查目标

- 类型：本地工作树全量审查
- 范围：`moview/`、`tests/`、`pyproject.toml`、`.gitignore`、`README.md`
- 审查时间：2026-07-10 10:35
- 约束：默认显示风格不变；新增外观操作不得触发轨道网格或 marching cubes 重算；Grid 大于 256 只警告、不限制。

### 审查输入证据

- 仓库状态：项目文件当前尚未提交，按完整工作树审查，不依赖 PR diff。
- 语法检查：`python -m compileall -q -x '(^|/)\._' moview tests` 通过。
- 基础测试：`python -m unittest discover -s tests`，18 项通过，其中 8 项因基础环境没有 GUI 依赖而按设计跳过。
- GUI 测试：Conda `wavefunction` 环境中以 offscreen Qt 运行，18 项全部通过。
- 依赖检查：基础环境与 Conda 环境 `python -m pip check` 均报告 `No broken requirements found`。
- 格式解析：四个本地波函数样例全部成功解析。
- 批处理：FCHK 与 Molden 的 Grid=16 轨道计算和正/负等值面提取均成功。
- 性能基线：Grid=56 四轨道 BasisGrid 路径约 1.25 s；分块 float32 路径约 1.31 s，但后者峰值内存显著降低。
- 默认分辨率：986 基函数样例的 Grid=81 单轨道 float32 分块计算约 2.90 s，进程峰值 RSS 约 142 MiB。
- 高分辨率：微型波函数 Grid=257 生成完整 `(257, 257, 257)`、16,974,593 点场，未再压缩到 900,000 点。
- 真实 GUI：在 macOS OpenGL 会话中检查了最小窗口、Display 页、FCHK 分子/等值面、标签和坐标轴。

### 问题清单

#### 严重问题

无。审查过程中发现的隐藏 Grid 点数上限、多 GiB BasisGrid、Auto 等值面缓存误用、GUI 主线程解析、快捷键抢占输入、异步加载竞态、Molden 越界系数静默忽略等问题均已修复并覆盖回归测试。

#### 建议修改

无阻断性建议。

#### 可选优化

**1：进一步拆分主窗口调度器**

- 位置：`moview/gui/main_window.py`
- 说明：布局已迁移到 `gui/layout.py`，但主窗口仍同时管理渲染任务、预取、缓存和相机。后续功能继续增长时，可将预取与场计算调度提取为独立 controller；本轮不继续拆分，以避免在已验证行为上增加重构风险。

**2：增加持续性能基准任务**

- 位置：`tests/`
- 说明：当前性能使用固定样例手动计时，未把耗时阈值写入单元测试，以避免不同 CPU/BLAS 环境造成不稳定失败。后续可在 CI 中增加仅记录不阻断的 benchmark。

### 已确认的设计边界

- ORCA 解析尚未实现，`parsers/orca.py` 只是明确抛出 `NotImplementedError` 的预留模块。
- 极高 Grid 经用户确认后仍可能超过物理内存；这是取消人工上限后的资源边界，GUI 会在计算前显示点数和内存估算。
- offscreen Qt 无真实 OpenGL context，因此自动测试覆盖控件和渲染项构造；真实 macOS 会话已补充视觉验证。

### 结论

- 结论：`Approved`
- 严重问题：0
- 建议修改：0
- 可选优化：2
- 下一步：可提交当前工作树；提交前无需额外代码修改。

---

## 2026-07-10 11:22 补充审查

### 范围

- 删除自动等值面并将 GUI/CLI 默认值统一为 `0.05`。
- 将 GUI 外观改为黑、白、中性灰与 `#8B5CF6` 紫色。
- 检查 Grid、Margin、Isovalue 改动后的后台预渲染、缓存复用和跨文件并发安全。

### 结论与证据

- 结论：`Approved`，未发现阻断性问题。
- 生产代码已无 `auto_iso`、自动等值面回退或固定 Grid 96 预渲染截止值。
- 参数改动采用 650 ms 防抖；后台任务按 192 MiB 标量场预算动态限制，最多 48 个轨道。
- 等值面变化复用已有标量网格；精确数值独立于滑块量化保存。
- BasisGrid 缓存按波函数文件代次隔离，旧线程任务不能写入当前文件缓存。
- 基础环境 24 项测试通过，其中 13 项 GUI 测试按依赖条件跳过；Conda GUI 环境 24 项全部通过。
- FCHK 与 Molden 的 HOMO 在 Grid=16、默认 `0.05` 下均生成非空正负等值面。
- Qt 窗口渲染确认 Compute/Display 页无控件重叠，Auto 控件已删除，科学颜色保持不变。

### 剩余边界

- 单个 float32 标量场超过 192 MiB 时只跳过后台预渲染，用户仍可确认风险后主动渲染。
- offscreen Qt 仍不能提供真实 OpenGL context；本轮已用真实 macOS Qt 窗口渲染补充视觉检查。

---

## 2026-07-10 12:45 配置与颜色补充审查

### 范围

- 新增 `moview.ini`、配置发现顺序、严格类型/范围校验和 CLI 覆盖规则。
- 检查 CPU 线程、后台批次、缓存预算、分块点数和 OpenGL 面数限制的实际接线。
- 检查命名 RGB 预设、GUI 正负相选择器、曲面重着色和两行顶部信息栏。

### 结论与证据

- 结论：`Approved`，未发现阻断性问题。
- 基础 Python 环境运行 33 项测试，18 项通过、15 项因缺少 GUI 依赖按设计跳过；Conda `wavefunction` 环境 33 项全部通过（含实际曲面 RGB 断言）。
- `python -m compileall -q -x '(^|/)\._' moview tests` 通过；两个环境的 `pip check` 均未发现损坏依赖。
- FCHK 与 Molden 样例均完成配置感知的 Grid=16 批处理和非空 HOMO 等值面提取。
- 配置加载拒绝非法 RGB、未知键、未知颜色和矛盾缓存预算；显式 CLI 参数优先于配置。
- 正负相颜色变化只复用已有 `SurfaceMesh` 重建 OpenGL 项，不调用 `submit_job`、网格求值或 marching cubes。
- 真实 macOS OpenGL 窗口确认自定义 `Mint`/`Violet` 曲面、顶部名称与色块同步；1040x680 最小窗口下控件和两行信息栏无截断。
- 资源并发仍受总预取场预算约束；缓存写入继续按波函数代次隔离，增加后台批次数不会绕过内存保护。

### 剩余边界

- 外置卷会生成已忽略的 `._*` AppleDouble 元数据；源码编译检查需要排除这些非 Python 文件，它们不会进入 Git。
- 用户可主动设置很高的线程数或内存预算；配置负责范围与关系校验，最终仍受物理 CPU、RAM 和显存限制。

---

## 2026-07-10 13:18 贴附原子标签补充审查

### 范围

- 新增 `label_placement = attached|floating` 配置、默认值和 GUI 选择器。
- 检查 Attached 标签的三维锚点、透视缩放、深度测试、原子表面偏移、批量绘制和资源释放。
- 确认 Floating 兼容模式、标签字号、原子风格/大小及轨道计算边界。

### 结论与证据

- 结论：`Approved`，未发现阻断性问题。
- 基础环境运行 34 项测试，18 项通过、16 项 GUI 测试按依赖条件跳过；Conda `wavefunction` 环境 34 项全部通过。
- 源码编译、CLI 帮助和两个环境的 `pip check` 均通过。
- 真实 macOS OpenGL 会话加载 94 原子样例，生成一个 94 标签纹理图集和一个 VBO；着色器编译、纹理上传和绘制后 `gl_error=0`。
- 旋转前后 framebuffer 差异为 9.353，视觉上标签始终锚定原子、随分子转动并按深度产生透视；前表面偏移使用当前原子风格与大小计算。
- Attached -> Floating 切换后旧纹理和 VBO 均已释放，替换项为原有 `AtomLabelItem`。
- 94 个 Number + element 标签的图集约 3.4 MiB、VBO 约 17.6 KiB；首次图集栅格化约 50-115 ms，LRU 命中后的附加视图约 0.35 ms。
- 标签默认仍为 Off，因此正常默认渲染不创建图集、VBO 或纹理；切换定位模式不会提交轨道网格或 marching cubes 任务。
- 1040x680 最小窗口下，标签内容与 Attached/Floating 选择器在同一行完整显示，没有增加设置面板高度。

### 已知取舍

- Attached 追求字面上的原子贴附，密集分子中标签可能重叠；需要自动避让时可切换 Floating。
- Attached 使用面向相机的三维 billboard：锚点、大小和遮挡服从三维变换，但字形保持正向，避免旋转到侧面后不可读。

---
